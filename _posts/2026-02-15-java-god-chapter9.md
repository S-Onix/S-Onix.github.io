---
title: 자바의 신 9장
date: 2026-02-15 20:04:28 +0900
categories: [도서, 자바의 신]  # 예: [개발, Python]
tags: [자바의 신, 자바]        # 예: [python, tutorial]
---

## 📌 소개

- 소개 : 자바의 신 9장 리뷰
- 목표 : 패키지와 임포트의 개념을 다시 숙지한다.

## 📝 본문

###  패키지 이름 주의사항

- 패키지 이름은 모두 소문자로 만든다
- 자바의 예약어를 사용하면 안된다
- `java, javax` 등은 Oracle 개발자가 아니면 사용해서는 안된다(기본 라이브러리 정보가 java... 로 시작하기 때문에)
- 일반적으로 비 영리단체는 `org`로 사기업은 `com`으로 패키지가 시작된다.



###  Import

- 다른 패키지에 있는 클래스들에 접근하기 위한 예약어

- 사용하고자 하는 클래스의 패키지 경로를 잘 찾아서 작성해야한다.

- 패키지 하단에 모든 클래스를 사용시에 `*`라는 문자를 마지막에 붙여 사용한다.

- Static Import >> static 변수와 메소드들을 클래스명을 붙이지 않고 사용 가능하다.

  ```java
  import static 패키지경로;
  ```



###  접근제어자

| 제어자                       | 같은 클래스 | 같은 패키지 | 자식 클래스(타 패키지) | 전체 |
| :--------------------------- | :---------- | :---------- | :--------------------- | :--- |
| **public**                   | O           | O           | O                      | O    |
| **protected**                | O           | O           | O                      | X    |
| **default**(package-private) | O           | O           | X                      | X    |
| **private**                  | O           | X           | X                      | X    |

- public : 누구나 접근 가능
- protedted : 같은 패키지 내의 혹은 상속 받은 클래스인 경우만 접근 가능
- default(package-private) : 같은 패키지 내의 클래스만 접근 가능
- private : 해당 클래스 내에서만 접근 가능



###  클래스 접근 제어자 유의사항

- 파일명과 동일한 클래스만 public 선언이 가능하다.

## 🎯 마무리

####  직접해 봅시다

- 생략



####  정리해 봅시다

1. 패키지 선언할 때 사용하는 예약어는 package 이다.



2. 패키지 선언은 소스의 가장 위에 위치해야만 한다.



3. 패키지의 이름은 java로 시작하면 안된다. 



4. 중간에라도 예약어가 패키지의 이름으로 선언되어도 안된다.



5. import는 클래스 선언 전에 명시되어 있어야만 한다.



6. 같은 패키지 내의 클래스와 java.lang 패키지에 있는 클래스는 별도로 import할 필요가 없다.



7.  `*`기호를 사용하면 해당 패키지에 있는 모든 클래스를 import한다. 예를 들어 java.util패키지에 있는 모든 클래스를 import하려면

import java.util.*;
로 선언하면 된다. 하지만, 이렇게 사용할 경우 import되는 클래스 이름들이 중복될 수 있으므로, 되도록이면 명시적으로 지정하는 것이 좋다. 

8.import static 을 사용하면 static한 변수를 쉽게 import할 수 있다. 

9. public 접근 제어자의 범위가 가장 넓으며, 어떤 클래스에서도 해당 클래스나 변수, 메소드에 접근 가능하다.



10. private로 선언할 경우에는 해당 클래스에서만 접근 가능하다. 



11. protected로 선언한 경우에는 상속받은 클래스만 접근 가능하다.



12. 자바 소스에는 파일명과 동일한 public으로 선언한 클래스가 존재해야만 한다. 
